import { DisponibilitePipe } from './disponibilite.pipe';

describe('DisponibilitePipe', () => {
  it('create an instance', () => {
    const pipe = new DisponibilitePipe();
    expect(pipe).toBeTruthy();
  });
});
